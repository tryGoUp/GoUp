# SA6002 ("storing non-pointer values in a sync.Pool allocates") does not apply
# here: the pools implement net/http/httputil.BufferPool, whose interface
# mandates []byte (Get() []byte / Put([]byte)), so a pointer-backed pool is not
# possible. Disable it repo-wide.
checks = ["all", "-SA6002"]
